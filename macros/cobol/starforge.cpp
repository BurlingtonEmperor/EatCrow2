#include <iostream>

#include "starforge.h"

int main(int argc, char *argv[]){
  if (argc > 1) {
    std::cout << "StarForge can only accept 1 file path as an argument.";
  } else {
    
  }

  return 0;
}